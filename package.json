{
  "name": "sooper-admin",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "assemble": "node scripts/assemble-control-panel.mjs",
    "dev": "npm run assemble && vite",
    "build": "npm run assemble && vite build",
    "preview": "npm run assemble && vite preview",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "@supabase/supabase-js": "latest",
    "vite": "latest",
    "typescript": "latest",
    "react": "latest",
    "react-dom": "latest",
    "lucide-react": "latest"
  },
  "devDependencies": {
    "wrangler": "latest"
  }
}
